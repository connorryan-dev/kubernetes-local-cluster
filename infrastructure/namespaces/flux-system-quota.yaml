apiVersion: v1
kind: ResourceQuota
metadata:
  name: flux-system-quota
  namespace: flux-system
spec:
  hard:
    limits.cpu: "5"
    limits.memory: 5Gi
    requests.cpu: "2"
    requests.memory: 2Gi
    pods: "30"

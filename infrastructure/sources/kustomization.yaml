apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# No sources yet — this will be populated if an external GitRepository is
# needed again. The github-runners source was removed once its manifests
# moved into apps/github-runners/ in this repo.
resources: []

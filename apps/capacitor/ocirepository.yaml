apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: capacitor
  namespace: flux-system
spec:
  interval: 24h
  url: oci://ghcr.io/gimlet-io/capacitor-manifests
  ref:
    # v0.4.8 is the final legacy Capacitor release; upstream moved to Capacitor Next.
    tag: v0.4.8

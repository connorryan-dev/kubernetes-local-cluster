apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: bento-dev
spec:
  type: NodePort
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
